def connect():
    user = "alanr"
    password = "london@1"
    database = "recorddb"
    host = "localhost"
    port = 13306
    return user, password, database, host, port


# Connecting to the Docker container
